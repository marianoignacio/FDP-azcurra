#include <stdio.h>
#define MaxFisico
int main(){
// EL vector se definde así y en los corchetes va la cantidad de variables que tenga el vector
    int vector[4]={0,1,2,3};
    // me está diciendo que tengo un vector de 4 posiciones, siempre se empieza desde el 0
    //*Si yo hago: 
    // si no pongo en los corchetes algún número se define el valor al declararla, osea despues del igual en las llaves 
    // pero igualmente es lo mismo que el primero
    int vector_a[]={0,1,2,3};
    // puedo cambiar los valores dentro de la llave y
    int vector_b[]={4,31,6,22}
    /* esto me dice que en el espacio 
    vector[0]=4
    vector[1]=31
    vector[2]=6
    vector[3]=22
    */
    //*Cómo accedo a un elemento?
    //  vector[posición]
    /*// todo arreglo de 100 elementos enteros int v_nros[100]
    // todo arreglo de 100 elementos reales float v_reales[100]
    // todo arreglo de 25 elementos tipo caracter char v_chars[25]
    // todo rreglo declarado por extensión int v_pares[] = {2,4,6,8,10}
*/
}

